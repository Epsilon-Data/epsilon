# Allow local connections inside the container (Unix socket, no SSL)
local   all             all                                     trust
# Require SSL for TCP connections
hostssl all             all             0.0.0.0/0               scram-sha-256
hostssl all             all             ::/0                    scram-sha-256